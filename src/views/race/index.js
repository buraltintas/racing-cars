import Container from '../car-selection/components/container';
import Ready from './components/ready';

const Race = () => {
  return (
    <Container>
      <Ready />
    </Container>
  );
};

export default Race;
